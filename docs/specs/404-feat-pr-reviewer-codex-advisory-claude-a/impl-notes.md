# Implementation Notes

## 確認事項

なし

## Implementation Notes

### Task 1
- 採用方針: `core_utils.sh` 末尾に `adj_log` / `adj_warn` / `adj_error` の 3 関数を既存 `pr_log` / `pi_log` と同形式（`[YYYY-MM-DD HH:MM:SS] [$REPO] adjudicator:` prefix）で追加。`issue-watcher.sh` Config ブロックの `PR_REVIEWER_STATUS_CHECK_ENABLED`（#349 / line 638-659）直後・`SECURITY_REVIEW_ENABLED`（#279 / line 661 以降）直前に「PR Reviewer Adjudicator 設定 (#404)」節を追加し、6 env を `${VAR:-default}` で解決。
- 重要な判断: (1) `ENABLED` の正規化は既存 `PR_REVIEWER_STATUS_CHECK_ENABLED` の `case … in true) … *) false ;;` パターンを踏襲（Req 5.5 既存規約整合）。(2) `EXEC_TIMEOUT` / `MAX_FINDINGS` の数値正規化は既存 `PR_REVIEWER_EXEC_FAIL_LIMIT`（issue-watcher.sh:603-611）と同じ `case ''|*[!0-9]*) … *) lt 1 ;;` イディオムを採用。(3) `FALLBACK_ON_FAIL` の既定は design.md「Architecture Decision: claude-review publisher contention」と「env var 仕様」表に従い `passthrough`（adjudicator SPOF 緩和 / 独立 Reviewer の verdict 尊重）に倒し、コメントに根拠を 1 段落で明記。(4) `MODEL` は空文字の場合に既定 `claude-sonnet-4-5` へ追加 fallback する明示分岐を入れた（`${VAR:-default}` だけだと `=""` 明示の場合に空文字が通る既知挙動の救済）。
- 残存課題: なし。本 task 終了時点で gate OFF 既定下の挙動は未変更（adjudicator.sh / REQUIRED_MODULES への登録は task 3 のスコープ。task 2 以降が `adj_log` / 6 env を消費する前提を整備済み）。検証結果: `bash -n` / `shellcheck` 警告ゼロ / 既存テスト 3 種（pr_publish_commit_status / pr_publish_claude_status_from_branch / pr_default_prompt）退行ゼロ / env 解決スモークで既定値 6 件 + 不正値正規化 4 件 + 合法 ON 値 5 件をすべて期待値どおり確認。

### Task 2
- 採用方針: `local-watcher/bin/adjudicator-prompt.tmpl` を 185 行で新規作成。冒頭コメントブロックは既存 `iteration-prompt.tmpl` / `triage-prompt.tmpl` の style（用途 / 配置先 / 依存 / 関連 / プレースホルダ一覧）に整合させ、本文は `pr_default_prompt`（pr-reviewer.sh:691）の単一波括弧 `{PR}` 規約に倣う。本文構造は「役割宣言 → 対象 PR メタ → REVIEW_TEXT 埋め込み → id 採番規約 → requirements.md 埋め込み → 分類基準（legitimate a-d / excessive 候補 a-d / 迷ったら legitimate）→ 全指摘 1:1 対応の義務 → read-only 制約 → JSON 出力契約」の順に並べた。
- 重要な判断: (1) **placeholder 規約の選定**: `iteration-prompt.tmpl` の `{{PR_NUMBER}}` 二重波括弧 style と `pr_default_prompt` の `{PR}` 単一波括弧 style のどちらに整合させるかが論点。adj は pr-reviewer.sh の bash パラメータ置換（`${var//\{PR\}/$pr}`）で扱う前提のため `pr_default_prompt` 側の単一波括弧に揃え、選定根拠を冒頭コメントの NOTE に明記。(2) **id 採番規約の明示**: design.md「Data Models」では `id: 1` 以降の整数を要求するが、adjudicator.sh 側 parse 結果に依存させると round-trip 不整合の余地が残る。本テンプレ内で「`## 指摘事項` 配下 bullet 行を登場順に id=1, 2, 3 と独立採番」を Claude 側に明示し、adj 側 parse 順序とは独立にした（design.md「Components and Interfaces」の `adj_extract_findings` 戻り値順と Claude 採番が一致する前提を明文化）。(3) **重複指摘の扱い**: design.md Open Q2「重複は別 finding として excessive 判定」を default としているため、本テンプレでも「集約はしない」「重複は別 finding として個別判定」を明示してプロンプト挙動を確定。(4) **「迷ったら legitimate」原則の根拠を本文に書く**: 単に「迷ったら legitimate」と命じるだけでなく、「実害指摘の見落とし（excessive 誤判定 → merge 後にバグ発覚）」が最悪 outcome、「legitimate 過判定は反復 round が 1 つ増えるだけ」が次悪 outcome であることを明示し、Claude が確信度の低い判定で excessive に倒すインセンティブを構造的に下げた（Req 1.4 の徹底）。(5) **コードフェンスで囲まない指示**: `claude --output-format json` が raw JSON を期待し、再 strip をしない呼び出しパスを前提に、出力本文を ` ``` ` で囲まないことを 2 箇所（read-only 制約節と JSON 出力契約節）で再強調。
- 残存課題: なし。本 task 終了時点で `adjudicator-prompt.tmpl` はディスク上に存在するが、参照する `adj_classify_findings` 本体（task 4）は未実装のため実行経路は発火しない。install.sh の既存 `copy_glob_to_homebin "$LOCAL_WATCHER_DIR/bin" "*.tmpl" "$HOME/bin"`（install.sh:1357）が glob で自動配布するため install.sh は touch せず、`grep -n 'tmpl' install.sh` で当該行が健在であることを確認済み。検証結果: `shellcheck` 警告ゼロ（.sh 群の非退行確認） / 既存テスト 3 種 PASS=178 FAIL=0 / `diff -r .claude/agents repo-template/.claude/agents` 差分ゼロ / `diff -r .claude/rules repo-template/.claude/rules` 差分ゼロ / placeholder 7 種（`{PR}` / `{SHA}` / `{BASE}` / `{HEAD}` / `{REVIEW_TEXT}` / `{SPEC_DIR}` / `{REQUIREMENTS_MD}`）すべてテンプレ内に存在することを `grep -oE '\{[A-Z_]+\}' ... | sort -u` で確認 / JSON schema キー 11 種（`decisions` / `summary` / `total` / `legitimate` / `excessive` / `verdict` / `reason` / `severity` / `id` / `file` / `line`）すべてテンプレ内に出現することを確認。

### Task 3
- 採用方針: `local-watcher/bin/modules/adjudicator.sh` を新規作成（200 行弱）。`adj_gate_enabled` は既に正規化済みの `PR_REVIEWER_ADJUDICATOR_ENABLED` を厳密 `=true` で評価する 5 行関数（重複正規化なし / Req 5.1）。`adj_extract_findings` は awk で `## 指摘事項` 配下 bullet 行を section 境界（次 `## ` 見出し / EOF）で正しく区切り、行頭 `- [(high|medium|low)] <file>:<line> — <内容>` の厳密 regex で parse して TSV 出力、jq で JSON 配列化。reconciliation check は bullet 総数 vs parse 件数を突合し、不一致時のみ `adj_warn` で WARN + rc=4（design.md Components and Interfaces 節の関数 contract そのまま）。`REQUIRED_MODULES` 配列の `"pr-reviewer.sh"` 直後に `"adjudicator.sh"` を追記（issue-watcher.sh:1329）。
- 重要な判断: (1) **awk 単一パスで 2 系統出力**: bullet 総数（reconciliation 用）と parse 成功 TSV を同じ awk スクリプトで 1 パス算出する設計を採用（review_text を 2 回 awk に流すより O(N) で済む）。awk 末尾 `END { printf "BULLET_TOTAL=%d\n", bullet_total }` で総数を尻に付け、bash 側で `grep -E '^BULLET_TOTAL='` と `grep -E '^PARSED\b'` で分離。(2) **section 境界の awk 状態機械**: `in_section = 1` を `^## 指摘事項[[:space:]]*$` で立て、`in_section && /^## /` で他の `## ` 見出しが来たら降ろす。これにより `## 結論` 後の bullet が誤って混入しないことを ケース 5 で確認。(3) **file:line 抽出の右端 colon 検索**: `<file>:<line>` の file 名にも path separator (`/`) が含まれ得るが `:` は含まない前提で、awk で末尾から `:` を走査し file / line に分割。line は `[0-9]+` 正規表現で再検証してから採用（防御的書式 drift 対応）。(4) **jq への未信頼入力渡し**: codex 出力（PR コメント由来 = 未信頼）は jq に `--arg` / `--argjson` で literal 渡し（CLAUDE.md §5 整合 / filter inline 展開禁止）。line は数値のため `--argjson` で int 型保持。(5) **`adj_warn` の遅延束縛**: adjudicator.sh は `adj_warn` を呼ぶが定義は core_utils.sh 側にある。bash の遅延束縛により呼び出し時に解決され、本モジュールは再定義しない（CLAUDE.md「機能追加ガイドライン §2」整合）。テスト側は `extract_function` で関数 1 つだけ隔離するため、test 冒頭で `adj_warn` を stub で潰す（fail-safe）。(6) **「指摘なし」プレーン行のみは reconcile 対象外**: codex の `指摘なし` テキストは bullet 行ではなく、awk の bullet カウントが 0 のため `bullet_total -gt 0` ガードで自然に rc=0 に倒れる（design.md の `[]` 返却契約と整合）。
- 残存課題: なし。本 task 終了時点で adjudicator.sh は REQUIRED_MODULES に登録済みで `source` 可能だが、`adj_run_for_pr` を呼ぶ配線（pr-reviewer.sh 末尾 hook）は task 6 のスコープのため、現状の watcher 起動経路では adjudicator 関数群は呼ばれない（NFR 2.1 既定 OFF 完全 no-op が維持される）。検証コマンドと結果: `bash -n local-watcher/bin/modules/adjudicator.sh` OK / `shellcheck local-watcher/bin/*.sh local-watcher/bin/modules/*.sh` 警告ゼロ / `bash local-watcher/test/adj_resolve_gate_test.sh` PASS=8 FAIL=0 / `bash local-watcher/test/adj_extract_findings_test.sh` PASS=23 FAIL=0 / 既存テスト 3 種 退行ゼロ（pr_publish_commit_status PASS=74 / pr_publish_claude_status_from_branch PASS=52 / pr_default_prompt PASS=52） / `diff -r .claude/agents repo-template/.claude/agents` / `diff -r .claude/rules repo-template/.claude/rules` 差分ゼロ。

### Task 4
- 採用方針: `local-watcher/bin/modules/adjudicator.sh` に `adj_classify_findings`（約 180 行）と `adj_validate_decisions`（約 70 行）を追記。`adj_classify_findings` は (i) `findings_json == []` / 空文字を早期 return（claude 起動しない / Req 1.4 観点で副作用ゼロ）、(ii) PR 番号 / SHA / base / head に shell metachar 検査 + 数値・hex 形式の strict 検証（pr_substitute_placeholders 流の防御）、(iii) `$HOME/bin/adjudicator-prompt.tmpl` を読みつつ `PR_REVIEWER_ADJUDICATOR_PROMPT` env で本文 override 可能（pr-reviewer.sh の `PR_REVIEWER_PROMPT` と同 semantics）、(iv) bash パラメータ展開で 7 placeholder（`{PR}` / `{SHA}` / `{BASE}` / `{HEAD}` / `{REVIEW_TEXT}` / `{SPEC_DIR}` / `{REQUIREMENTS_MD}`）を置換、(v) mktemp で prompt / out / err の 3 tempfile を作り RETURN trap で削除、(vi) `timeout claude -p ... --output-format json --permission-mode plan --model ...` を直接 invoke（`eval` 不使用 / `--permission-mode plan` で Bash/Edit/Write を Claude 側で構造的にブロック / design.md Security Considerations 第 4 項 = bypassPermissions 不使用と整合）、(vii) 実行直後 `git status --porcelain` で workspace-modified 検出時は `git checkout -- .` + rc=3、(viii) `jq -r '.result // empty'` で SDK 標準出力 `{"type":"result",...,"result":"..."}` から本文を抜き、awk で `{...}` を雑に切り出してから `jq -e '.'` で valid JSON 検証 + stdout 出力。`adj_validate_decisions` は decisions_json が JSON object かつ `.decisions` array / `.summary` object であることに加え、件数一致 / verdict ∈ {legitimate, excessive} / id が 1〜N 連番 / summary 集計 vs decisions verdict 集計の一致を jq filter で連続検証する。
- 重要な判断: (1) **引数構成の拡張（4 → 6 引数）**: design.md interface 表は厳密 4 引数だが、`{BASE}` / `{HEAD}` placeholder を tmpl に流し込むため `base_ref` / `head_ref` を引数 5,6 として追加した。pr-reviewer.sh の `pr_execute_review_command` でも同様の契約拡張の前例（design 表より引数多い）が impl-notes に記録されており、本 task でも同じ正当化を行う。(2) **`{REVIEW_TEXT}` 流し込み方式 = 候補 B（findings_json 再構成）**: prompt 文字列構築のための呼び出し元拡張（review_text 全文を引き継ぐ案 A）と、findings_json から `## 指摘事項\n- [sev] file:line — message\n...` を再構成する案 B のうち、**B を採用**。理由は (a) design.md interface 表 4 引数の拡張幅を最小化（review_text を持ち回らない）、(b) stub claude テストで findings_json のみで擬似実行可能（テスト容易性）、(c) adjudicator-prompt.tmpl の指示は「`## 指摘事項` 配下 bullet 行を登場順に id=1, 2, 3 と独立採番」であり、findings_json の順序保持と整合する。trade-off として codex の `## 概要` / `## 結論` 散文が prompt に渡らないが、adjudicator の判定根拠は bullet 行（指摘内容）と requirements.md（AC）であり、散文の欠落は分類精度に影響しない。(3) **`--permission-mode plan` 採用**: design.md Security Considerations 第 4 項に従い `bypassPermissions` を使わず、Claude 側で Bash / Edit / Write を構造的にブロックする `plan` モードを採用。これは tmpl 本文の read-only 制約宣言（Bash / Edit / Write を使わない）の defense-in-depth として機能する。(4) **`eval` 不使用**: security-review.sh の `SECURITY_REVIEW_CLAUDE_CMD` 経路は運用者 override 用 cmd template の bash -c 化に `eval` を回避しているが、adjudicator では tmpl の cmd 化 env が存在しないため `claude` を直接呼ぶ単純経路にした（CLAUDE.md「禁止事項」と整合）。(5) **claude 出力からの JSON 抽出 awk 関数**: `claude --output-format json` の `.result` フィールドは生 JSON 文字列を期待するが、tmpl 指示違反で前置き散文や code fence が混入する case に備え、最初の `{` から depth=0 で閉じる `}` までを awk で抜き出す防御層を追加した。これがケース 4（散文のみ）で rc=2 への倒し込みを担保する。(6) **`adj_validate_decisions` の id 連番 sanity check**: adjudicator-prompt.tmpl の id 採番規約「登場順に 1, 2, 3 と独立採番」と整合するため、`sort` + `range(1; N+1)` 比較で連番を検証。Claude が id を 0 始まり / 飛び番号で返す逸脱を早期検出できる。(7) **stub claude / stub git / stub timeout の PATH 注入**: 本番コードに「テスト専用の env override 経路」を追加しない方針（method A）を採用。tempdir に stub `claude` / `git` / `timeout` を chmod 755 で配置し、test 冒頭で `PATH="$TMP_BIN:$PATH"` する。`timeout` も stub する理由は test 環境に GNU `timeout` が無いケースを避けるため。`git` stub は read-only invariant 検査の `git status --porcelain` を常に「clean」に返すよう no-op 化。(8) **stub claude が `--output-format json` の SDK 標準形式を返す**: `{"type":"result","subtype":"success","result":"<embedded JSON 文字列>"}` を jq で組み立て、`.result` フィールドに本来 Claude が生成する adjudicator JSON 本文を文字列として埋め込む。これにより adj_classify_findings 内の `jq -r '.result // empty'` 経路がテストで正常動作する。(9) **fallback モード（`PR_REVIEWER_ADJUDICATOR_FALLBACK_ON_FAIL`）は本 task で参照しない**: 関数 contract は rc を返すだけで、`passthrough` / `legitimate` の選択は呼び出し元 `adj_run_for_pr`（task 6 スコープ）の責務。テストではこの前提を踏まえて 4 種 rc（0=ok / 1=exec 失敗 / 2=JSON parse 失敗 / 3=workspace-modified）が明示的に分離されることを assert する。
- 残存課題: なし（本 task スコープ内）。task 5 で `adj_apply_label_decision` / `adj_apply_status_decision` を追加する際、`adj_classify_findings` の rc=3 workspace-modified を呼び出し元がどう扱うか（再試行 / 即 fallback）の判断が必要。task 6 の `adj_run_for_pr` 実装時に `PR_REVIEWER_ADJUDICATOR_FALLBACK_ON_FAIL` を読み、rc=1/2/3 をすべて fallback モードに従わせる予定。検証コマンドと結果: `bash -n local-watcher/bin/modules/adjudicator.sh` OK / `bash -n local-watcher/test/adj_classify_test.sh` OK / `shellcheck local-watcher/bin/*.sh local-watcher/bin/modules/*.sh local-watcher/test/adj_classify_test.sh` 警告ゼロ / `bash local-watcher/test/adj_classify_test.sh` PASS=27 FAIL=0 / `bash local-watcher/test/adj_resolve_gate_test.sh` PASS=8 FAIL=0 / `bash local-watcher/test/adj_extract_findings_test.sh` PASS=23 FAIL=0 / 既存テスト 3 種 退行ゼロ（pr_publish_commit_status PASS=74 / pr_publish_claude_status_from_branch PASS=52 / pr_default_prompt PASS=52）。

### Task 5
- 採用方針: `local-watcher/bin/modules/adjudicator.sh` 末尾に `adj_apply_label_decision`（約 40 行）/ `adj_read_reviewer_verdict`（約 70 行）/ `adj_apply_status_decision`（約 55 行）/ `adj_post_decision_comment`（約 100 行）の 4 関数を追記。近接テスト `local-watcher/test/adj_publish_decision_test.sh` は既存 `adj_classify_test.sh` / `pr_publish_claude_status_from_branch_test.sh` の `extract_function` + `eval` イディオムを踏襲し、stub gh / stub git / stub timeout を bash 関数として宣言する方式（PATH 注入ではなく同一プロセス内 stub）で 51 件 assert を組んだ。`pr_publish_claude_status` / `pr_publish_commit_status` / `pr_status_check_enabled` を pr-reviewer.sh から同時抽出して同一プロセスに読み込み、`adj_apply_status_decision` の publish 経路全体を通せるようにした。
- 重要な判断: (1) **Reviewer 先行優先の実装手段（design.md「Architecture Decision: claude-review publisher contention」/ Behavior contract 2）**: `adj_read_reviewer_verdict` を `adj_apply_status_decision` の publish 直前で呼び、戻り値 `"reject"` 検出時に `legitimate_count` に依らず `result="reject"` に倒す。`reject` 以外（`approve` / `""` 不在 / RESULT 行不在）の 3 パターンを同一経路（legitimate 件数のみで verdict 決定）に集約することで「Reviewer 不在 = adjudicator が判定責任を持つ」境界を明示。(2) **`adj_read_reviewer_verdict` の git 経路**: 既存 catch-up `pr_publish_claude_status_from_branch`（pr-reviewer.sh:1294-1389）の解決手順（head_ref から issue 番号抽出 → `git ls-tree --name-only origin/<head_ref> -- docs/specs/` → `<N>-` 始まり最初の dir 採用 → `git cat-file -e` 存在確認 → `git show` で内容取得）をそのまま流用。`parse_review_result` のように file path を取る関数は使わず、本関数内で `extract_review_result_token`（issue-watcher.sh:209）と同じ正規表現 `RESULT:[[:space:]]+(approve|reject)([^[:alnum:]_]|$)` で grep する独立実装にした。理由は (a) parse_review_result は Findings 抽出も担うが本関数は verdict のみで十分（軽量化）、(b) review-notes.md のフォーマット drift 耐性は word boundary 正規表現側に依存させる方が catch-up と境界が分かりやすい。(3) **`adj_post_decision_comment` の重複防止 = adjudicator 専用 inline 検査**: tasks.md の「重複防止判定は既存 `pr_already_processed` 流用」指示を素直に読むと `pr_already_processed` を直接呼ぶ案だが、既存実装は marker prefix が `idd-claude:pr-reviewer` 固定（pr-reviewer.sh:259）であり、adjudicator marker `idd-claude:pr-adjudicator` には流用できない。同関数の構造（`gh api /issues/<n>/comments` + jq の test 検査 + 取得失敗時は安全側で既存扱い skip）を **同形で本関数内にインライン展開**し、prefix 部分のみ `idd-claude:pr-adjudicator sha=<sha>` + `kind=decision` のリテラル + `--arg sha` で組み立てた（CLAUDE.md §5 jq filter inline 展開禁止整合）。impl-notes に判断根拠として記録。(4) **label 冪等性の保証**: `gh pr edit --add-label` / `--remove-label` は既存 `pr_add_iteration_label`（pr-reviewer.sh:1003）が明示的に「既付与で冪等 no-op」と運用してきた前例があり、`gh pr view --json labels` で現状確認してから add/remove に分岐するパターンは取らない（API 呼び出し 1 回に削減して観測ログ規約 NFR 1.1 を遵守 / pr_add_iteration_label と同方針）。(5) **未信頼入力の防御層**: `adj_post_decision_comment` の comment body 組み立て箇所では `pr_number` / `sha` / `severity` / `file` / `line` / `reason` / `message` を全て jq の `--arg` / `--argjson` で受けるか、本文側で printf に変数展開する場合は事前検証済みの値（`pr_number` `sha`）と string 形式（`severity` 等）のみを使用。jq filter 内に `${var}` の直接展開は無い（重複判定 jq クエリも `--arg sha` でリテラル渡し）。(6) **入力検証の早期 reject**: 後段 `pr_publish_commit_status` でも sha / pr_number / state は再検証されるが、`adj_apply_status_decision` 冒頭で先に検証することで、Reviewer 先行優先の git 経路（数百 ms かかり得る）に進む前に reject。`adj_apply_label_decision` も同様に gh 呼び出し前に検証（gh への無駄な API call 抑止）。(7) **claude-review publish 経路の target_url 選択**: catch-up 経路（pr_publish_claude_status_from_branch）は review-notes.md の blob URL を組み立てるが、adjudicator 経路は Reviewer verdict と adjudicator 判定の合成のため blob URL が意味的に弱い。PR URL を流用（`pr_publish_codex_status` と対称 / codex-review と同じ semantics）。(8) **excessive 個別 marker の投稿失敗は致命的でない**: summary 投稿成功後、excessive 個別 marker の投稿が失敗しても summary は既に立っており、`pi_general_filter_excessive`（task 7）の self-filter キーが 1 件欠ける程度の影響（その finding は iteration 入力に残るが Developer が legitimate のみに集中できる本来の目的への影響は限定的）。WARN 1 行残して継続する経路にした（design.md Error Handling との整合）。
- 残存課題: なし（本 task スコープ内）。task 6 の `adj_run_for_pr` 実装時の留意点: (a) `adj_classify_findings` rc=3 workspace-modified を fallback として扱う際は `legitimate` モードでも `passthrough` モードでも本 task の 4 関数のうち `adj_apply_status_decision` / `adj_apply_label_decision` / `adj_post_decision_comment` を呼ばずに早期 return すべき（adjudicator 自身の SPOF 緩和）。(b) `adj_apply_status_decision` 内の Reviewer 先行優先は task 6 側で再度呼ぶ必要はない（DRY 原則）。検証コマンドと結果: `bash -n local-watcher/bin/modules/adjudicator.sh` OK / `bash -n local-watcher/test/adj_publish_decision_test.sh` OK / `shellcheck local-watcher/bin/*.sh local-watcher/bin/modules/*.sh local-watcher/test/adj_publish_decision_test.sh` 警告ゼロ / `bash local-watcher/test/adj_publish_decision_test.sh` PASS=51 FAIL=0 / 既存テスト 6 種 退行ゼロ（adj_resolve_gate PASS=8 / adj_extract_findings PASS=23 / adj_classify PASS=27 / pr_publish_commit_status PASS=74 / pr_publish_claude_status_from_branch PASS=52 / pr_default_prompt PASS=52） / `diff -r .claude/agents repo-template/.claude/agents` / `diff -r .claude/rules repo-template/.claude/rules` 差分ゼロ。

### Task 6
- 採用方針: `local-watcher/bin/modules/adjudicator.sh` 末尾に `adj_resolve_spec_dir_from_head_ref`（約 35 行 / head_ref から `docs/specs/<N>-<slug>/` 絶対パスを `git ls-tree` で解決する read-only helper、catch-up 経路の同じ手順を流用）/ `adj_log_summary`（5 行 / Req 4.2 一行サマリ）/ `adj_synthesize_all_legitimate_decisions`（約 20 行 / fallback `legitimate` モードで全 finding を legitimate 扱いに倒す合成 JSON 生成）/ `adj_run_for_pr`（約 200 行 / オーケストレータ本体）を追記。`pr-reviewer.sh` への変更は (a) `pr_run_review_for_pr` 末尾 `pr_publish_codex_status` の直後に adjudicator hook 4 行（`|| adj_warn ...` で吸収）を追加、(b) `process_claude_review_status_catchup` ループ内 `pr_publish_claude_status_from_branch` の直前に catch-up suppression 4 行を追加、(c) ファイル末尾に `pr_catchup_should_defer_for_adjudicator`（約 50 行 / gate ON + adjudicator marker `<!-- idd-claude:pr-adjudicator sha=<sha> -->` 存在判定 read-only helper）を追記。近接テスト 2 本（`adj_integration_no_op_test.sh` PASS=44 / `pr_catchup_suppression_test.sh` PASS=17）を追加し、PATH 注入 stub（gh / claude / git / timeout）でのトレース観測と関数 stub 経由の挙動切り替え（GH_STUB_MODE / MATCH_SHA env）を組み合わせた。
- 重要な判断: (1) **fallback モードの集約 = `adj_synthesize_all_legitimate_decisions` の導入**: `adj_run_for_pr` 内で reconciliation mismatch（rc=4）/ classify 失敗（rc=1/2/3）/ validate 失敗（rc=1）の 3 経路すべてで「`PR_REVIEWER_ADJUDICATOR_FALLBACK_ON_FAIL` を読み、`legitimate` なら全件 legitimate 合成 / `passthrough` なら skip」という同じ分岐を繰り返すため、合成ロジックを別 helper に切り出して呼び出し元の重複を最小化した。design.md interface 表には未掲載の helper だが、設計判断レベルでは fallback モード semantics（Error Handling 節）の自然な落とし所であり、独立性希薄化は招かない。(2) **early-return 配置**: gate OFF → return 0 を関数冒頭 / `adj_gate_enabled` の直後に配置し、NFR 2.1「副作用ゼロ・log 行ゼロ」を構造的に保証。続けて入力検証（pr_number ^[0-9]+$ / sha ^[0-9a-f]{7,40}$）を行い、これも非ゼロ exit を伝搬せず（Invariants）`adj_warn` 1 行後に return 0 で抜ける。後段 `adj_apply_*` でも検証されるが、gh / claude 起動前に reject することで NFR 1.1 観測ログ抑制を担保。(3) **spec_dir 解決の局所 helper 化（impl-notes 推奨どおり）**: `adj_read_reviewer_verdict`（task 5）は head_ref 内で同等の git 経路を持つが、本関数の戻り値は verdict 文字列のみで spec_dir を露出しない。`adj_classify_findings`（task 4）は spec_dir_hint を受けるが解決ロジックは持たないため、本 task で新規 `adj_resolve_spec_dir_from_head_ref` を導入して `adj_run_for_pr` 冒頭で 1 度だけ解決する形に統一（DRY / catch-up 経路と semantically 等価）。pwd 基準で絶対化（`adj_classify_findings` 内の `[ -d "$spec_dir_hint" ]` 検査が cwd 非依存で通る）。(4) **catch-up suppression の `gh pr view --json comments --jq` 採用**: design.md では `gh pr view --json comments` で fetch して bash 側で marker 検査と書かれているが、実装では `--jq '.comments[].body'` を使い改行区切りの body 列を直接取得する形に簡略化（API レスポンス全体を bash に持ち回らない / メモリ効率）。`--jq` の query は固定文字列で未信頼入力を含まないため filter inline 展開禁止規約（CLAUDE.md §5）と非衝突。marker 検査は `grep -F -q -- "idd-claude:pr-adjudicator sha=<sha>"` で固定文字列マッチ（`grep -F` で正規表現解釈を抑止、`--` でオプション解釈打ち切り）。(5) **catch-up suppression の安全側挙動**: gh 取得失敗 / 入力検証失敗のいずれも rc=1（catch-up 続行）に倒す設計を採用（design.md Behavior contract 4 と整合）。「adjudicator marker 不在として扱う」≒「catch-up が引き継ぐ」という passthrough fallback と semantically 同じ振る舞いになり、merge gate が単一 publisher 原則を守りつつも cell 失敗で永久 BLOCKED にならない（SPOF 緩和）。(6) **test stub 戦略の使い分け**: `adj_integration_no_op_test.sh` は PATH 注入で gh / claude / git stub を配置し（gate ON 経路で本物の `gh pr edit` 等が呼ばれた場合の trace を記録）、`pr_catchup_should_defer_for_adjudicator` テストは bash 関数 override で gh stub を切り替える方式を採用。前者は「複数バイナリの trace を file に集約」「環境依存（claude / timeout の存在）を排除」が目的、後者は「シナリオ別の挙動切り替え（GH_STUB_MODE env で 5 種類）」が目的で、両者の最適化が異なるためあえて手段を変えた。(7) **`grep -c` の pipefail 対応**: 当初 `grep -c '^gh ' "$GH_CALL_LOG" 2>/dev/null || echo 0` で書いていたが、grep が「ファイルは存在するがマッチゼロ」の場合に rc=1 で `echo 0` 経路に倒れ、`set -euo pipefail` 配下で `0` + grep stdout `0` の二重出力で `"00"` が `gh_count` に入る病理を観測。`$( { grep -c ... || true; } | tail -n 1)` の subshell 二段に変更して回避した（test 限定の問題で本体コードには影響なし）。
- 残存課題: なし（本 task スコープ内）。task 7（pi 側 self-filter 拡張）の実装時の留意点: `adj_post_decision_comment` が投稿する excessive marker `<!-- idd-claude:pr-adjudicator-excessive id=<N> sha=<sha> -->` の prefix `pr-adjudicator-excessive` は本 task の catch-up suppression marker `pr-adjudicator sha=...` と異なる（excessive 個別 marker は sha が prefix 部に含まれない）ため、`pi_general_filter_excessive` は本 marker 全件除外で良い（sha 一致判定は不要）。task 8 README の Q1 注記（impl PR のみスコープ、設計 PR は別 Issue）も task 6 でのコード変更とは独立。検証コマンドと結果: `bash -n local-watcher/bin/modules/adjudicator.sh local-watcher/bin/modules/pr-reviewer.sh` OK / `shellcheck local-watcher/bin/*.sh local-watcher/bin/modules/*.sh install.sh setup.sh .github/scripts/*.sh` 警告ゼロ / `bash local-watcher/test/adj_integration_no_op_test.sh` PASS=44 FAIL=0 / `bash local-watcher/test/pr_catchup_suppression_test.sh` PASS=17 FAIL=0 / 既存テスト 7 種 退行ゼロ（adj_resolve_gate PASS=8 / adj_extract_findings PASS=23 / adj_classify PASS=27 / adj_publish_decision PASS=51 / pr_publish_commit_status PASS=74 / pr_publish_claude_status_from_branch PASS=52 / pr_default_prompt PASS=52） / `diff -r .claude/agents repo-template/.claude/agents` / `diff -r .claude/rules repo-template/.claude/rules` 差分ゼロ。

### Task 7
- 採用方針: `local-watcher/bin/modules/pr-iteration.sh` に `pi_general_filter_excessive`（5 行本体 / 関数定義位置は chain 順序対応で `pi_general_filter_event_style` の直前）を追加。gate ON (`PR_REVIEWER_ADJUDICATOR_ENABLED=true` 厳密一致) 時は `jq '[.[] | select((.body // "") | contains("idd-claude:pr-adjudicator-excessive") | not)]'` で marker 含むコメントを除外、それ以外は `jq '.'` pass-through。`pi_collect_general_comments` の filter chain は `self → resolved → excessive → event_style → truncate` の 5 段に拡張し、`after_resolved` 直後・既存 `filter_event_input` 算出の前に `after_excessive` 段を挟む。サマリ 1 行ログ（pi_warn / pi_log 両経路）に `filtered_excessive=${filtered_excessive}` を `filtered_resolved` と `filtered_event` の間に挿入し、`filtered_event` の計算式を `count_resolved - count_event` から `count_excessive - count_event` に修正。近接テスト `local-watcher/test/pi_general_filter_excessive_test.sh`（14 ケース PASS）を追加。
- 重要な判断: (1) **gate 判定の配置**: 呼び出し元 `pi_collect_general_comments` 側で gate 判定して chain 段数を可変にする案と、本関数内で判定して chain を常時 5 段に固定する案の選択。後者を採用（**chain 段数固定 + gate OFF は pass-through**）。理由は (a) 呼び出し元が gate 状態を意識せず常に 5 段呼び出しできるため pr-iteration.sh 側の制御フロー分岐が増えない、(b) サマリ 1 行ログに常時 `filtered_excessive=N` が出るため観測ログ書式が gate 状態に依らず一貫する（Req 4.4 ログ規約整合 / 運用者が gate 状態に関係なく filtered_excessive を grep できる）、(c) 既存 `adj_gate_enabled`（adjudicator.sh）と同様に `${PR_REVIEWER_ADJUDICATOR_ENABLED:-false}` を厳密 `=true` 一致で判定する規約と整合（重複正規化なし / Req 5.1）。trade-off として gate OFF 時にも `jq '.'` のサブシェルコストを 1 回追加で支払うが、watcher 全体の cron tick 周期から見れば誤差レベル。(2) **既存 `filtered_event` 計算式の修正**: `pi_collect_general_comments` のサマリは `filtered_X = count_前段 - count_当段` という差分形式で書かれており、chain に excessive 段を挿入したことで `filtered_event` の前段が `count_resolved` から `count_excessive` に変わる。task 6 の learning にあった「task 7 で chain 挿入時の留意点」に対応して、本 task の diff で `filtered_event=$((count_excessive - count_event))` に書き換えた（chain 順序を変えた以上、差分式も整合させないと gate OFF 時に `filtered_event` が常に 0 になるのは同じだが、ON 時に excessive で抜けた件数が `filtered_event` 側に混入して観測ログが誤読される）。(3) **substring 衝突の予防的検証**: design.md「Hidden Marker 形式」表で `pr-adjudicator` prefix が `pr-iteration` self-filter と前方一致しない旨が宣言されているが、本 task の Case 5（gate ON + `idd-claude:pr-iteration` を含むコメント）と Case 7（gate ON + `idd-claude:pr-adjudicator sha=... kind=decision` 単独 marker）を独立に並べて、self-filter prefix `pr-iteration` も decision サマリ marker `pr-adjudicator sha=` も excessive filter を pass through することを観測可能にした。これにより task 5 の `adj_post_decision_comment` が投稿する 2 種類の marker（サマリ + excessive 個別）のうち、サマリは pi 側 iteration agent に渡り、excessive 個別だけが除外される設計の意図がテストで保護される。(4) **`extract_function` 互換**: 関数本体は `if … then … else … fi` の 3 行構成で書き、単独行 `}` を関数末尾にのみ置いた。`pi_general_filter_self_test.sh` と同じイディオムで `awk` 抽出 → `eval` 可能。テスト側は `extract_function` で `pi_general_filter_excessive` と `pi_general_filter_self` を別途 source し、Case 5（self prefix と非衝突）の検証で両者を同時に持つコメントが pi_general_filter_self で除外され、pi_general_filter_excessive 経路には到達しないという chain 上の境界も観測。(5) **gate ON での pr-adjudicator decision サマリの保持**: Case 7 で `<!-- idd-claude:pr-adjudicator sha=... kind=decision -->` 単独 marker を pass-through させる仕様を明示テスト化した。adjudicator の裁定サマリコメント自体は iteration agent に渡って良い情報（運用者・Developer が「なぜこの round では excessive 指摘が iteration から外れたか」を後から追える観測材料）であり、excessive 個別 marker `pr-adjudicator-excessive` だけが除外対象である境界を design.md「Hidden Marker 形式」表の意図どおりにテストで保護した。
- 残存課題: なし（本 task スコープ内）。task 8（README 反映）の実装時の留意点: 本 task で追加したサマリログ書式（`filtered_excessive=N` が `filtered_resolved` と `filtered_event` の間に常時出る）を、README「PR Reviewer Adjudicator (#404)」節の「観測可能性」段落で言及することを推奨。task 9（静的解析 / E2E スモーク）では `local-watcher/test/pi_general_filter_excessive_test.sh` が `## Verify` 構造化ブロックの存在ガード `[ -f ... ]` 経路で実行されることを確認すれば良い（tasks.md:179）。検証コマンドと結果: `bash -n local-watcher/bin/modules/pr-iteration.sh` OK / `shellcheck local-watcher/bin/*.sh local-watcher/bin/modules/*.sh` 警告ゼロ / `bash local-watcher/test/pi_general_filter_excessive_test.sh` PASS=14 FAIL=0 / 既存テスト 11 種 退行ゼロ（pi_general_filter_self PASS=17 / pr_publish_commit_status PASS=74 / pr_publish_claude_status_from_branch PASS=52 / pr_default_prompt PASS=52 / adj_resolve_gate PASS=8 / adj_extract_findings PASS=23 / adj_classify PASS=27 / adj_publish_decision PASS=51 / adj_integration_no_op PASS=44 / pr_catchup_suppression PASS=17） / `diff -r .claude/agents repo-template/.claude/agents` / `diff -r .claude/rules repo-template/.claude/rules` 差分ゼロ。

### Task 8
- 採用方針: `README.md` の「オプション機能一覧（opt-in、既定 OFF）」表（line 1478-1502 周辺）に `PR Reviewer Adjudicator` 行を 1 行追加（`PR_REVIEWER_Commit Status Publishing (#349)` 行の直後に挿入し、PR Reviewer 系を集約。表内の行順は機能カテゴリ別並びを踏襲し、`#番号` 順では並んでいない既存規約に整合）。新規 h2 節「PR Reviewer Adjudicator (#404)」を `## PR Reviewer Processor (#261)` 節の終端 `---` 直後 / `## PR Reviewer Commit Status Publishing (#349)` 節の直前（line 2980 周辺）に挿入し、design.md「Scope clarification」「Open Questions Q1」「Architecture Decision: claude-review publisher contention」「env var 仕様」「Latency / cost stacking」相当をまとめた約 256 行構成（冒頭 limitation 段落 + Mermaid 風 ASCII 図 + 完全な opt-in 段落 + 動作概要 6 段 + env var 表 6 行 + fallback モード解説 + consumer 手順 4 段 + トレードオフ Req 6.1 / 6.2 / 6.3 各 1 段落 + 観測可能性 + FAQ 5 項目 + Migration Note）。
- 重要な判断: (1) **新規節の挿入位置**: 「`## PR Reviewer Processor (#261)` の後に挿入」というタスク指示に対し、「直後」と「`## PR Reviewer Commit Status Publishing (#349)` の直前」の 2 通りの解釈が可能だが、本 README の既存節順序が「`#261` → `#349`」と論理依存順（adjudicator は #261 の出力を入力に取り、#349 と並列に `claude-review` status を扱う）であるため、`#261` → `#404` → `#349` の挿入を採用。`#404` adjudicator が `claude-review` を publish する semantic を `#349` 節より前に説明することで、`#349` の `claude-review` publish と adjudicator の publish の関係性が読み手に取って意味的に通る順序になる。(2) **オプション機能一覧表での挿入位置**: 表内の既存行順序は `Promote / Phase C / Auto Rebase / ... / PR Reviewer (#261) / PR Reviewer Commit Status (#349) / Security Review (#279) / ...` と「機能カテゴリ別」並びで `#番号` 順ではない（例えば #261 が #279 より後の機能だが、`PR Reviewer` 系として集約配置）。本 task では PR Reviewer 系を集約する既存規約に従い、`#349` 行の直後（PR Reviewer 系の末尾）に `#404` 行を追加。`Security Review (#279)` 行の前に位置することで、PR Reviewer 関連 3 行（`#261` / `#349` / `#404`）が表内で隣接する。(3) **limitation 段落（Q1）の冒頭配置**: design.md「Scope clarification」（L21-25）と Open Questions Q1（L765-792）の双方が「impl PR のみスコープ / 設計 PR は別 Issue / consumer 暫定運用は branch protection 解除 or 人間 admin merge」を明示しており、ae-mdm 設計レビュー #3 への対応として本機能 README の **冒頭部** に limitation 段落を置くことが本 task の指示。動作概要・env var・観測可能性等の本論より先に limitation を読ませる順序にすることで、本機能を有効化しただけでは設計 PR ゲート問題が解消されないことを運用者が見落としにくくした。(4) **トレードオフ節の Req 6.1 / 6.2 / 6.3 1 段落ずつ明示**: タスク指示「Req 6.1 = 独立性希薄化、Req 6.2 = 誤 bypass 緩和策で Req 1.4 / 4.1 を参照、Req 6.3 = 100% 精度を目標としない」に対し、Req 6.2 の 1 段落内に **AC 1.4 と AC 4.1 を明示的に参照する文** を入れた（「AC 1.4（迷ったら legitimate）」「AC 4.1（裁定根拠の観測可能性）」というラベルを段落本文中に箇条書きで埋め込み、機械検索でも追跡可能にした）。Req 6.3 は「LLM 裁定の本質的限界として 100% 精度を目標としない」「ae-mdm のような明らかな過剰指摘パターンを大幅に減らす実用精度を目指す」「100% 精度を要求する運用には本機能は適していない」を明示。(5) **FAQ で「latency / cost への影響」を Req 5.5 / NFR 1.1 観点で具体化**: design.md「Latency / cost stacking」（L178-182）の指示に従い、codex 最大 1800s + adjudicator 最大 300s で increase、`PR_REVIEWER_MAX_PRS` / `PR_REVIEWER_ADJUDICATOR_EXEC_TIMEOUT` で上限抑制、flock 排他下での他処理待機、cron tick 既定 2 分との関係、`PARALLEL_SLOTS`（#16）で slot 並列化が補助手段になる旨を明記。(6) **`adj_log_summary` 書式の README 公開（task 7 learning 引き継ぎ）**: task 7 learning にあった「task 8 README の実装時の留意点: 本 task で追加したサマリログ書式（`filtered_excessive=N` が `filtered_resolved` と `filtered_event` の間に常時出る）を、README『PR Reviewer Adjudicator (#404)』節の『観測可能性』段落で言及することを推奨」に対応して、observability 節に `grep 'pr-iteration:' cron.log | grep filtered_excessive=` の操作例まで含めた。`adjudicator:` プレフィックス側のサマリログ書式（`adjudicator: decision pr=#<N> sha=<sha8> total=<N> legitimate=<N> excessive=<N> fallback=<mode>`）も併記し、両ログを運用者が時系列で追える形にした。(7) **既存節 (`#261` / `#349`) の意味改変回避**: 既存 PR Reviewer Processor (#261) 節 と PR Reviewer Commit Status Publishing (#349) 節の本文は **完全に無変更**。本 task では (a) オプション機能一覧表に 1 行追加、(b) `#261` 末尾の `---` の直後・`#349` 冒頭の `## PR Reviewer...` 直前に新規節を挿入、の 2 箇所の編集のみで完結した。
- 残存課題: なし（本 task スコープ内）。task 9（静的解析・既存テスト退行確認・E2E スモーク）の実装時の留意点: README は対象外（shellcheck / actionlint / bash -n / 既存テスト退行確認は task 9 のスコープだが、本 task は README のみのため static analysis 対象外）。本 task で追加した README の anchor link `#pr-reviewer-adjudicator-404` は GitHub Flavored Markdown の auto-generated anchor 規約（h2 見出しを lowercase + space → hyphen 化）に従い、表内 `[PR Reviewer Adjudicator (#404)](#pr-reviewer-adjudicator-404)` のリンクが該当 h2 に到達することを `grep -n '^## PR Reviewer' README.md` で 3 件（#261 / #404 / #349）見出しの存在確認済み。検証コマンドと結果: `grep -n '^## PR Reviewer' README.md` 3 件（#261=L2746 / #404=L2983 / #349=L3239）/ `grep -n 'PR_REVIEWER_ADJUDICATOR' README.md` 表行 1 件 + 新規節 13 件 = 計 14 件出現 / `diff -r .claude/agents repo-template/.claude/agents` 差分ゼロ / `diff -r .claude/rules repo-template/.claude/rules` 差分ゼロ。既存テスト退行確認は本 task では未実施（task 9 のスコープ。README のみの編集で .sh ファイル / shellcheck / bash -n の対象外）。

### Task 9
- 採用方針: コード修正は発生せず、静的解析・既存テスト退行確認・新規テスト一括実行・cron-like 最小 PATH 起動確認・dry run NFR 2.1 完全 no-op 確認・root ↔ repo-template 同期 diff 確認の 6 系統を順次実施。task 1-8 で各 task 完了時に同等の検証を実施してきたため、本 task は spec 全体の最終 sanity check として位置付ける。
- 重要な判断: (1) **dry run の REPO_DIR 準備**: `REPO_DIR=/tmp/scratch-issue-404-task-9` に `git init` のみ実施し `origin` remote 未設定で起動。watcher は `git fetch origin` 等が失敗しても `2>/dev/null` で吸収する設計のため、stderr に `fatal: 'origin' does not appear to be a git repository` が漏れるが exit 0 で正常完了することを確認（NFR 2.1 観点では adjudicator: log の有無のみが判定対象）。(2) **gate OFF と env unset の両系統で NFR 2.1 を確認**: `PR_REVIEWER_ADJUDICATOR_ENABLED=false` 明示と `env -u PR_REVIEWER_ADJUDICATOR_ENABLED` の 2 通りで `adjudicator:` log line count = 0 を確認し、`${PR_REVIEWER_ADJUDICATOR_ENABLED:-false}` の既定値正規化が機能していることまで担保。(3) **cron-like 最小 PATH での `claude` 未解決は設計上の前提**: `env -i HOME=$HOME PATH=/usr/bin:/bin` 経路では `claude` は `(not found)` だが、これは `issue-watcher.sh` 冒頭で `$HOME/.local/bin` 等を PATH に prepend する設計上の前提であり、修正対象ではない。`gh` / `jq` / `flock` / `git` の 4 種が `/usr/bin/*` で解決可能であることを確認したことが本 step の達成条件。(4) **既存テスト `pr_publish_claude_status_from_branch_test.sh` の "command not found" メッセージは PASS 範囲内**: `extract_function` で `pr_publish_claude_status_from_branch` 関数のみを単独抽出して実行する際、task 6 で追加した `pr_catchup_should_defer_for_adjudicator` helper を source していないため "command not found" メッセージが 3 行出る。これはテスト側の `... && pr_catchup_should_defer_for_adjudicator ... || ...` の `||` フォールバック経路で吸収され、PASS=52 FAIL=0 で通過する想定通りの挙動（task 6 で当該 helper を pr-reviewer.sh 末尾に追記した際の境界設計）。本 task で修正対象とはしない。
- 残存課題: なし（本 spec の最終 task のため次 task への持ち越し事項なし）。検証コマンドと結果（task 9 実施分）:
  - `shellcheck local-watcher/bin/*.sh local-watcher/bin/modules/*.sh install.sh setup.sh .github/scripts/*.sh` 警告ゼロ（rc=0）
  - `actionlint .github/workflows/*.yml` クリーン（rc=0）
  - `bash -n` 全対象ファイル（issue-watcher.sh / adjudicator.sh / pr-reviewer.sh / pr-iteration.sh / core_utils.sh / install.sh / setup.sh）OK
  - 既存テスト退行ゼロ: `pr_publish_commit_status_test.sh` PASS=74 FAIL=0 / `pr_publish_claude_status_from_branch_test.sh` PASS=52 FAIL=0 / `pr_default_prompt_test.sh` PASS=52 FAIL=0（計 PASS=178 FAIL=0）
  - 新規テスト一括実行 PASS=167 FAIL=0: `adj_classify_test.sh` PASS=27 / `adj_extract_findings_test.sh` PASS=23 / `adj_integration_no_op_test.sh` PASS=44 / `adj_publish_decision_test.sh` PASS=51 / `adj_resolve_gate_test.sh` PASS=8 / `pi_general_filter_excessive_test.sh` PASS=14
  - cron-like 最小 PATH: `gh=/usr/bin/gh` / `jq=/usr/bin/jq` / `flock=/usr/bin/flock` / `git=/usr/bin/git` 解決 OK、`claude=(not found)` は設計上の前提（PATH prepend 経由で解決）
  - dry run NFR 2.1: `PR_REVIEWER_ADJUDICATOR_ENABLED=false` 明示でも env unset でも `adjudicator:` log line count = 0 を確認、watcher exit 0
  - `diff -r .claude/agents repo-template/.claude/agents` 差分ゼロ / `diff -r .claude/rules repo-template/.claude/rules` 差分ゼロ

## AC Traceability

| Requirement | 担保方法 |
|---|---|
| Req 5.1（opt-in gate / 安全側正規化） | `PR_REVIEWER_ADJUDICATOR_ENABLED` の `case true) ... *) false` 正規化、`FALLBACK_ON_FAIL` の `legitimate|passthrough` 以外を `passthrough` に倒す `case`、数値 env の `case ''|*[!0-9]*) ... lt 1` 正規化（env 解決スモークで `True` / `invalid_value` / `abc` / `-5` がすべて既定値に倒れることを確認） |
| Req 5.3（既存 env 名・既定値・意味の不変性） | 既存 env 名（`PR_REVIEWER_STATUS_CHECK_ENABLED` / `SECURITY_REVIEW_ENABLED` / 周辺）は touch せず、新規 6 env のみ追加。既存テスト 3 種が PASS=178 で退行ゼロを確認 |
| Req 5.5（既存 exit code・ログ stderr/stdout 契約の不変性） | `adj_log` は stdout、`adj_warn` / `adj_error` は `>&2`。既存 `pr_log` / `pi_log` / `sec_log` 群と同一の関数シグネチャを保持。Config 節は env 解決と正規化のみで exit code を変更しない |
| Req 4.4（ログ prefix・timestamp 書式の既存規約整合） | 3 関数の出力書式が `[YYYY-MM-DD HH:MM:SS] [$REPO] adjudicator:` で既存 `pr_log` 等と byte レベルで揃う（diff 確認） |

## 検証コマンドと結果

| コマンド | 結果 |
|---|---|
| `bash -n local-watcher/bin/modules/core_utils.sh` | OK |
| `bash -n local-watcher/bin/issue-watcher.sh` | OK |
| `shellcheck local-watcher/bin/modules/core_utils.sh local-watcher/bin/issue-watcher.sh` | 警告ゼロ |
| `bash local-watcher/test/pr_publish_commit_status_test.sh` | PASS=74 FAIL=0 |
| `bash local-watcher/test/pr_publish_claude_status_from_branch_test.sh` | PASS=52 FAIL=0 |
| `bash local-watcher/test/pr_default_prompt_test.sh` | PASS=52 FAIL=0 |
| env 解決スモーク（既定値 / 不正値 / 合法 ON 値の 3 系統） | 既定 6 env 期待値一致、不正値 4 件すべて既定に正規化、合法 ON 値 5 件透過 |

STATUS: complete
