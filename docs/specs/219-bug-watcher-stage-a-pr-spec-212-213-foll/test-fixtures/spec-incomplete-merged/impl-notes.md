# 実装ノート

> spec 成果物完全性保証（#219）のスモークテスト用 fixture（#216 再現 / 補完対象ケース）。
> impl-notes.md のみが存在し requirements.md / review-notes.md が欠落している状態を再現する。
> 先行 impl PR が MERGED 済みのとき、`_spec_missing_artifacts` は `requirements review` を
> 列挙し、`spec_artifacts_completeness_guard` が docs-only 補完追従 PR を起動することを期待する。

STATUS: complete
